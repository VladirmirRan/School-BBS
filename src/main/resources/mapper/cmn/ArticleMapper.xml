<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.bbs.mapper.ArticleDomainMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.school.bbs.domain.ArticleDomain">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="summary" property="summary"/>
        <result column="category_id" property="categoryId"/>
        <result column="thumbnail" property="thumbnail"/>
        <result column="is_top" property="isTop"/>
        <result column="status" property="status"/>
        <result column="view_count" property="viewCount"/>
        <result column="comment" property="comment"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id
        , title, content, summary, category_id, thumbnail, is_top, status, view_count, comment, create_by, create_time, update_by, update_time, deleted
    </sql>

    <select id="queryArticleList" resultType="com.school.bbs.domain.ArticleDomain">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        article
        WHERE
        title = #{title}
        AND deleted = 0
        AND status = 2
        <if test="startTime != null">
            AND create_time &gt; #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt; #{endTime}
        </if>
    </select>

</mapper>
